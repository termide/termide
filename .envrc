# Nix users get the pinned dev shell from flake.nix. A plain rustup checkout
# (macOS, or any machine without Nix) would otherwise fail here with
# "use flake: command not found", so fall back to putting cargo on PATH.
if has nix; then
  use flake
else
  PATH_add "${CARGO_HOME:-$HOME/.cargo}/bin"
fi
