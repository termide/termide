[package]
name = "termide-agent-acp"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "ACP client for the termide coding agent: an external agent over stdio behind the same panel"

[dependencies]
log.workspace = true
serde_json.workspace = true

termide-agent-core = { path = "../agent-core" }
termide-agent-mcp = { path = "../agent-mcp" }

[dev-dependencies]
tempfile.workspace = true
